// Sequelize configuration to prevent migrations
const path = require('path');

module.exports = {
  'config': path.resolve('src/config', 'database.js'),
  'models-path': path.resolve('src', 'models'),
  'seeders-path': false,
  'migrations-path': false,
  // Disable all migrations
  'migrationStorage': 'none',
  'seederStorage': 'none'
};